const menuBtn = document.querySelector(".nav-burger"); let menuOpen = !1; menuBtn.addEventListener("click", () => { menuOpen = menuOpen ? (menuBtn.classList.remove("open"), !1) : (menuBtn.classList.add("open"), !0) });